parentCompany: Company --nullable
code: String --Mandatory
name: String --Mandatory
countryCode: String Mandatory
externalSystem: String --nullable
externalId: String --nullable
logoUrl: String --nullable
sites: Set<Site> --nullable
timeZones: Set<CompanyTimeZone> --nullable
isSystemCompany: Boolean --nullable
validFrom: LocalDate --nullable
validTo: LocalDate --nullable

package com.os360.enterprise.dto;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.Valid;
import java.time.LocalDate;
import java.util.UUID;

import com.fasterxml.jackson.annotation.JsonFormat;
import jakarta.validation.constraints.AssertTrue;

public class CompanyCreateRequest {

```
// Optional parent company UUID
private UUID parentCompany;

@NotBlank(message = "Code is mandatory")
private String code;

@NotBlank(message = "Name is mandatory")
@Size(max = 300, message = "Name cannot exceed 300 characters")
private String name;

@NotBlank(message = "Country code is mandatory")
@Pattern(regexp = "^[A-Z]{2}$", message = "Country code must be 2 uppercase letters")
private String countryCode;

private String externalSystem;

private String externalId;

private String logoUrl;

// Optional
// @Valid
// private Set<SiteCreateRequest> sites;

// Optional
// @Valid
// private Set<CompanyTimeZoneCreateRequest> timeZones;

private Boolean isSystemCompany;

@JsonFormat(pattern = "yyyy-MM-dd")
private LocalDate validFrom;

@JsonFormat(pattern = "yyyy-MM-dd")
private LocalDate validTo;

// Cross-field validation: validTo >= validFrom
@AssertTrue(message = "validTo must be after or equal to validFrom")
public boolean isValidDates() {
    if (validFrom == null || validTo == null) {
        return true; // skip check if one of them is null
    }
    return !validTo.isBefore(validFrom);
}

// Getters and Setters (or use Lombok @Getter/@Setter)
```

}

